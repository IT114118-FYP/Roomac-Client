{
  "name": "react-app-client",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix web-client",
    "postinstall": "npm install --prefix web-client",
    "build": "npm run build --prefix web-client"
  },
  "engines": {
    "node": "12.x"
  }
}
